{{#each this}}
<div class="row bg-peach justify-content-center pb-3">
    <div class="col-5 bg-royal">
        <p class="text-peach"> {{comment_text}}</p>
    </div>
    <div class="col-5 bg-royal">
        <p class="text-peach text-end"> {{user.username}} on {{format_date created_at}}</p>
    </div>
</div>
{{/each}}