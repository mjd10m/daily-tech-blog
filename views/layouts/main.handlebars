<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Daily Tech Blog</title>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm bg-royal">
            <div class="container-fluid">
            <a class="navbar-brand text-peach" href="#">The Daily Tech Blog</a>
            <div class="collapse navbar-collapse row" id="navbarNavAltMarkup">
                <div class="navbar-nav row">
                    <div class="col-11">
                        <div class="row px-3">
                            <a class="nav-link col-auto text-peach" href="/">Home</a>
                            <a class="nav-link col-auto text-peach" href="/dashboard">Dashboard</a>
                        </div>
                    </div>
                    <div class="col-1">
                        <div class="row">
                            {{#if loggedIn}}
                            <a class="nav-link text-end text-peach" href="" id="logout">Logout</a>
                            {{else}}
                            <a class="nav-link text-end text-peach" href="/login">Login</a>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>
            </div>
        </nav>
    </header>
    <main>
        {{{body}}}
    </main>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
</body>
</html>